# QF Framework (local wheel)
qf @ file:///app/dist/qf-1.0.2-py3-none-any.whl

# API / framework
flask>=3.0.0
flask-restx>=1.3.0
gevent>=23.9.0
psycogreen>=1.0.2

# QF framework transitive deps
redis>=5.0.0
kafka-python>=2.0.2

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Elasticsearch — pin to v8.x to match ES 8.13 server
elasticsearch>=8.13.0,<9.0.0

# LLM — OpenAI-compatible client (points at Ollama)
openai>=1.30.0

# RAG / in-memory search
rank-bm25>=0.2.2

# Embeddings — CPU-only ONNX runtime for fastembed (GPU stays owned by SGLang)
fastembed>=0.3.0

# Config / utils
python-dotenv>=1.0.0
requests>=2.31.0
colorama>=0.4.0

# File ingestion (Phase 1: csv/tsv/json/jsonl/xlsx/txt/md)
charset-normalizer>=3.3.0
openpyxl>=3.1.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
responses>=0.25.0
faker>=25.0.0
